# Global proxy buffer sizes for upstream apps (Next.js, auth cookies, etc.).
# Included from /etc/nginx/conf.d/ — applies to all reverse-proxied locations.
#
# Fixes: upstream sent too big header while reading response header from upstream

proxy_buffer_size 128k;
proxy_buffers 4 256k;
proxy_busy_buffers_size 256k;
